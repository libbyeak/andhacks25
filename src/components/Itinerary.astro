---
import { Icon } from "astro-icon/components"; /* not used but maybe they will be */
import { Image } from "astro:assets";
const images = import.meta.glob<{ default: ImageMetadata }>(
  "/src/assets/*.{jpeg,jpg,png,gif}"
);
---
<section class=" bg-pblue border-1 border-black shadow-card group hover:shadow-hover transition rounded-xl m-5 p-5">
  <div class="grid row-start-1">
    <div class="flex gap-0 justify-start items-start">
      <h3 class="text-6xl">&hacks XI Itinerary</h3>
    </div>
  </div>
  <div class="grid row-start-2">
    <div class="flex md:flex-row flex-col justify-content justify-items px-4 md:px-0 gap-10 md:gap-0">
      <div class="flex flex-col gap-0 justify-start items-start md:w-1/2 p-0px">
        <p class="text-3xl">Important Dates</p>
      </div>
      <div class="flex flex-col md:w-1/2 object-contain items-start justify-start group gap-0 p-0px">
        <p class="text-3xl">Fun Events</p>
            <div class="h-80 w-60 overflow-hidden relative -mt-30 rounded-2xl">
              <!-- put something here I guess, probably? maybe it's useful? -->
            </div>
            </a>
          </div>
      </div>
      </div>
</section>